# Agent Development

This project was built with the microsoft-foundry skill. Before working on or answering questions about Foundry agents, read the microsoft-foundry skill first.

Use the `azd` hosted-agent workflow for deployments. Validate .NET container builds as `linux/amd64`; do not publish local macOS ARM artifacts for Foundry.